name: rukito
description: "Sistema de Monitoreo de Cadena de Frío - Rukito"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.0.0
  
  # API & HTTP
  http: ^1.1.0
  
  # Local Storage
  shared_preferences: ^2.2.0
  
  # Date & Time
  intl: ^0.19.0
  
  # Charts
  fl_chart: ^0.65.0
  
  # Utilities
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  
  # assets:
  #   - assets/
  
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700
